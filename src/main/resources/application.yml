spring:
  datasource:
    url: jdbc:mysql://mysql:3306/urlshortener?createDatabaseIfNotExist=true
    username: root
    password: boody
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: redis
      port: 6379

  cache:
    type: redis

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration